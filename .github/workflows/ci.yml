name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-and-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Confidential data guard
        run: npm run security:confidential-data

      - name: Install dependencies
        run: npm ci

      - name: Validate schema-only baseline
        run: npm run validate:schema-baseline

      - name: Unit tests
        run: npm test

      - name: Type-check and build
        run: npm run build

      - name: Dependency audit (high severity)
        run: npm audit --audit-level=high
        continue-on-error: true
